[package]
name = "minimal"
version.workspace = true
edition.workspace = true

[dependencies]
bevy = {workspace = true}
bevy_dioxus_sync = {workspace = true}
dioxus = {workspace = true, default-features = false, features = ["native", "launch"]}
dioxus-desktop = {workspace = true}
frontend = {path = "./frontend"}