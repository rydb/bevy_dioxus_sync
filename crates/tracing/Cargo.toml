[package]
name = "bevy_dioxus_tracing"
version.workspace = true
edition.workspace = true

[dependencies]
macro-v = {workspace = true}
tracing = {workspace = true, optional = true}

[features]
trace = ["dep:tracing"]